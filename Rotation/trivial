#include <stdio.h>
#include <string.h>

#define MAX 1000

char *reverse(int d, char *str1)
{
   char reverse_str[MAX] = {0, );
   int len = strlen(str1);
   int i;

   if(d>0)
   {
       for(i = 0; i< len; i++)
       {
   	    reverse_str[i] = str1[len-1-i];

       }
       return reverse_str;
   }
   else if(d<0)
